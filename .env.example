DATABASE_URL="postgresql://aiscrum:aiscrum@localhost:5432/aiscrum"
AUTH_SECRET="change-me-to-a-long-random-string"
# Public URL of the app (Railway sets this after first deploy — use https://your-app.up.railway.app)
NEXTAUTH_URL="http://localhost:3000"
APP_URL="http://localhost:3000"
AUTH_TRUST_HOST="true"
CRON_SECRET="dev-cron-secret"
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM="AI Scrum Master <noreply@localhost>"
OPENAI_API_KEY=
# Set to require when your Postgres needs SSL (Railway usually provides this via DATABASE_URL)
# PGSSLMODE=require
