# Playwright E2E

## Commands

```bash
npm install
npx playwright install chromium
npm run test:e2e
npm run test:e2e:headed
npm run test:e2e:ui
```

Uses seeded users (`admin@acme.local` / `password123`, etc.). Reuses an existing server on port 3000 when available.
